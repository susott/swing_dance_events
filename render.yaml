databases:
  - name: postgres
    ipAllowList: []
    databaseName: swingfestival_production
    user: swingfestival

services:
  - type: web
    name: swingfestival
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false